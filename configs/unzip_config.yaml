input_dir:  "jobs/{download_timestamp}/output"  # filled by runner or pass --vars
output_dir: "jobs/{timestamp}_unzip/output"
logs_dir:   "jobs/{timestamp}_unzip/logs"
workers:    6
delete_compressed: false
